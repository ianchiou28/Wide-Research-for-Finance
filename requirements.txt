feedparser==6.0.11
openai>=1.0.0
python-dotenv==1.0.1
pyyaml==6.0.1
APScheduler==3.10.4      # 替代 schedule，支持线程池和 cron 表达式
requests==2.31.0
beautifulsoup4==4.12.3
flask==3.0.0
gunicorn==21.2.0         # 生产级 WSGI 服务器

# 新增依赖 - 实时数据和扩展功能
flask-socketio==5.3.6    # WebSocket支持
flask-cors==4.0.0        # 跨域支持
eventlet==0.35.1         # 异步支持

# 回测和价格数据
akshare>=1.12.0          # A股数据
yfinance>=0.2.0          # 美股数据

# 测试
pytest>=7.0.0
pytest-cov>=4.0.0